# Ephemeral, per-run dedup base written by scan/prescan_guard.py
# (union of origin/main + open scan branches). Regenerated every scan; never committed.
scan/backlog_cases.json

# Python bytecode cache
__pycache__/
*.pyc

# per-run scan artifacts
scan/retrieval_log.json
scan/nyt_wapo_verification.txt
scan/source_report.txt
